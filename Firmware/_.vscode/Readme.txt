These are example settings for using VS Code to compile Arduino code.

The important settings are those found in c_cpp_properties.json (choose the proper configuration for your device) and the output path specified in arduino.json.
